// src/pantallas/JoinTeamScreen/JoinTeamScreen.styles.ts

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#ffffff',
  },
  text: {
    fontSize: 20,
    fontWeight: 'bold',
  },// src/pantallas/JoinTeamScreen/JoinTeamScreen.functions.ts

  // Funciones relacionadas con unirse a un equipo irán aquí.
  // Por ahora, no hay funciones específicas.
});
